import WorkspaceExplorer from "@/components/workspace/WorkspaceExplorer";

export const metadata = { title: "Workspace — Bhati AI Agent" };

export default function WorkspacePage() {
  return <WorkspaceExplorer />;
}
